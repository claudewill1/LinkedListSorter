using System;

namespace LinkedListSorter.App.Utilities
{
    public static class InputUtility
    {
        // Reads an integer from the user with a prompt
        public static int GetIntFromUser(string prompt)
        {
            // TODO: Display prompt, read input, parse to int, handle invalid input
            return 0;
        }

        // Reads a menu choice from the user
        public static int GetMenuChoice()
        {
            // TODO: Display menu, read selection, and return choice
            return 0;
        }
    }
}
